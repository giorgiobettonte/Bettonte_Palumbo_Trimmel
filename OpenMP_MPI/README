OpenMP combined with MPI version

Modules to load:
  module load intel

To compile:
  mpicxx -O2 -fopenmp -o openmp_mpi openmp_mpi.cpp

To run:
  sbatch openmpmpi_job.sh

To see execution:
  cat slurm-<number_job>.out

